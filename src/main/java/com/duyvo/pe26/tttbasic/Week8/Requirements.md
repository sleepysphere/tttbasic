For the sake of simplicity, assume that the human always starts first.

In this case, "single-user" means that only one user can play  against the computer at the same time. However:

The server is a server: it does not need to be restarted after every game.
[Corollary: Different users can be "connected" to the server at the same time,
but to play with the computer any user will need to "wait" until the user currently playing with the computer (if any) has finished his/her game.]
Don't use threads (it is not needed).